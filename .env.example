NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
GEMINI_API_KEY=your-gemini-api-key

UPSTASH_REDIS_REST_URL=xx
UPSTASH_REDIS_REST_TOKEN=xx

STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_ID_MONTHLY=price_...
STRIPE_PRICE_ID_SEMESTER=price_...

# -----------------------------------------------------------------------------
# DDoS (proxy): general request rate limit — applies to ALL requests (pages, API, RSC).
# Production always on; dev: set ENABLE_RATELIMIT=true to test.
# -----------------------------------------------------------------------------
# Anonymous (by IP): one page load can trigger many requests; 60/60s avoids 429 on first load.
RATE_LIMIT_PUBLIC_REQUESTS=60
RATE_LIMIT_PUBLIC_WINDOW="60 s"
# Logged-in (by userId)
RATE_LIMIT_PRO_REQUESTS=100
RATE_LIMIT_PRO_WINDOW="10 s"
# ENABLE_RATELIMIT=true

# -----------------------------------------------------------------------------
# LLM: chat/LLM API access only — daily quota + per-window rate limit.
# Enforced in QuotaService (e.g. /api/chat/stream, chat actions).
# -----------------------------------------------------------------------------
# Daily quota (per user per day)
LLM_LIMIT_DAILY_FREE=10
LLM_LIMIT_DAILY_PRO=30
# Per-window rate limit (free: ≤ daily so 3/60s; pro: e.g. 60/60s)
RATE_LIMIT_LLM_FREE_REQUESTS=3
RATE_LIMIT_LLM_FREE_WINDOW="60 s"
RATE_LIMIT_LLM_PRO_REQUESTS=60
RATE_LIMIT_LLM_PRO_WINDOW="60 s"

# File upload
NEXT_PUBLIC_MAX_FILE_SIZE_MB=10

# Gemini AI models (optional)
# GEMINI_CHAT_MODEL=gemini-2.5-flash
# GEMINI_PARSE_MODEL=gemini-2.0-flash
# GEMINI_EMBEDDING_MODEL=gemini-embedding-001
